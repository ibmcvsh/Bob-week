I would build a SPA to demo a pill tracker application.

Please create 3 sample users, each with different prescriptions.

This is a demo, so just have a picklist at the top for each user.

For each user tell them their prescriptions & if its time to take a pill.

Include the current time and a button to advance time by 8 hours. so that i can demo how the tracker notifies the user that a pill is due.

Include a button to mark the pill as taken.

Include a simplified description of what the pill does